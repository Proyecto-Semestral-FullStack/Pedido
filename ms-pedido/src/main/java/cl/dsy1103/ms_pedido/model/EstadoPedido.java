package cl.dsy1103.ms_pedido.model;

// Enum simple que representa los estados del pedido
public enum EstadoPedido {
    PENDIENTE,
    CONFIRMADO,
    EN_PROCESO,
    ENVIADO,
    ENTREGADO,
    CANCELADO
}